<dl class="form">
  <dt>
    <%= label 'Name', :for => :name %>
  </dt>
  <dd>
    <%= text_field :name %>
  </dd>
  <dt>
    <%= label 'Home Page', :for => :home %>
  </dt>
  <dd>
    <%= text_field :home %>
  </dd>
  <dt>
    <%= label 'description', :for => :description %>
  </dt>
  <dd>
    <%= text_area :description, :rows => 5 %>
  </dd>
  <% if session.user && session.user.admin? %>
    <dt>
      <%= label "Repository", :for => :repos %>
      Ex) git://github.com/{user id}/{project name}.git
    </dt>
    <dd>
      <%= text_field :repos %>
    </dd>
  <% end %>
  <dt>
    Ready to register?
  </dt>
  <dd>
    <%= submit 'Register Plugin', :class => :submit %>
  </dd>
</dl>
